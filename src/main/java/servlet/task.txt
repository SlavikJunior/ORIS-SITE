Создать класс сервлета, который:
1) При GET-запросе возвращает HTML-страницу, на которой есть тег <form>
2) При POST-запросе принимает данные из этой формы (например, логин, email, пароль, сообщение, etc).
3) Проводит базовую валидацию (поля не пустые, email содержит @) ИМЕННО НА СТОРОНЕ СЕРВЕРА (просто <input type="email"> не пойдет!.
4) В случае ошибки возвращает на ту же страницу с сообщением об ошибке.
5) В случае успеха сохраняет данные в простой текстовый файл на сервере (вспоминаем IO с первого курса) и перенаправляет на страницу "Спасибо!".

📊 Итоговое сравнение
Уровень	Что делаешь сам	Когда использовать
Сокеты (TCP/UDP)	Всё: протокол, парсинг, заголовки	Игры, чаты, кастомные протоколы
HTTP-сервер	Определяешь хендлеры, сам формируешь ответ	Лёгкие REST API, микросервисы
Сервлеты	Только бизнес-логику (doGet/doPost)	Веб-приложения, Java EE, Spring Boot